# MediaGrabber environment defaults
# Duplicate this file to `.env` and adjust values for your environment.

# ============================================
# 應用設定
# ============================================

# Maximum number of concurrent ffmpeg workers handling transcode jobs.
MG_MAX_TRANSCODE_WORKERS=2

# Root directory for per-job artifacts (relative paths resolve from repo root).
MG_OUTPUT_DIR=output

# Time-to-live (seconds) for retaining progress snapshots in memory.
MG_PROGRESS_TTL_SECONDS=300

# ============================================
# 日誌設定
# ============================================

# 日誌存放目錄（相對於專案根目錄或絕對路徑）
MG_LOG_DIR=backend/logs

# 日誌級別：DEBUG, INFO, WARNING, ERROR, CRITICAL
MG_LOG_LEVEL=INFO

# 日誌格式：json, text
MG_LOG_FORMAT=text

# 單個日誌檔案最大大小（bytes，預設 10MB）
MG_LOG_MAX_BYTES=10485760

# 日誌檔案備份數量
MG_LOG_BACKUP_COUNT=5

# ============================================
# Flask 設定（開發模式）
# ============================================

# Flask 監聽地址
FLASK_HOST=127.0.0.1

# Flask 監聽端口
FLASK_PORT=8080

# Flask 環境：development, production
FLASK_ENV=development
